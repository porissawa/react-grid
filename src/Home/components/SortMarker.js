import React from 'react';
import styled from 'styled-components';

// styled components
const Marker = styled.div`

`;

function SortMarker({className, isActive, behaviour}) {
  return (
    
  )
}

export default SortMarker;